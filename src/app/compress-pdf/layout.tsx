import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Compress PDF Free Online - Reduce PDF File Size | EditPDFs.app",
  description: "Compress PDF files for free online. Reduce PDF file size while maintaining quality. No signup required. Your files never leave your browser.",
  keywords: "compress pdf, reduce pdf size, shrink pdf, free pdf compressor, pdf optimizer",
};

export default function CompressPDFLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
