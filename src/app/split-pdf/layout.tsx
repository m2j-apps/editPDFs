import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Split PDF Free Online - Extract Pages from PDF | EditPDFs.app",
  description: "Split PDF files for free online. Extract specific pages or split into multiple documents. No signup required. Your files never leave your browser.",
  keywords: "split pdf, extract pdf pages, separate pdf pages, free pdf splitter, divide pdf",
};

export default function SplitPDFLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
