import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign PDF Free Online - Add Signature to PDF | EditPDFs.app",
  description: "Sign PDF documents for free online. Draw, type, or upload your signature. Add signatures and initials to any PDF. No signup required, 100% private.",
  keywords: "sign pdf, add signature to pdf, esign pdf, free pdf signature, digital signature pdf",
};

export default function SignPDFLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
